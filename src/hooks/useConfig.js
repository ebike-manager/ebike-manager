// useConfig is provided via React Context to share a single state across all components.
// This re-export keeps all existing imports working without any changes.
export { useConfig } from '../contexts/ConfigContext';
